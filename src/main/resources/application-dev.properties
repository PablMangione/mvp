# src/main/resources/application-dev.properties

# Base de datos local de desarrollo
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/acainfodb?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:7leDeli8}

# Mostrar SQL en desarrollo
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging más detallado
logging.level.com.acainfo.mvp=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# Sesiones más largas en desarrollo
server.servlet.session.timeout=2h

# Swagger siempre activo en dev
springdoc.swagger-ui.enabled=true